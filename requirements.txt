# QSO Predictor v2.0 Requirements
# Copyright (C) 2025 Peter Hirst (WU2C)

# Core dependencies
PyQt6>=6.4.0
paho-mqtt>=2.0.0
numpy>=1.24.0
requests>=2.28.0
psutil>=5.9.0              # Process memory diagnostics in analyzer cache health log (v2.5.5+)

# Local Intelligence Engine
scipy>=1.10.0              # Statistical analysis (correlation, etc.)
pandas>=2.0.0              # Data manipulation for log analysis

# ML Training (optional - only needed for model training)
scikit-learn>=1.3.0        # ML models (RandomForest, LogisticRegression)
joblib>=1.3.0              # Model serialization

# IONIS Propagation Engine (v2.4.0)
safetensors>=0.4.0         # Model checkpoint loading (numpy backend)

# Audio Doctor (v2.6.0) — Windows Core Audio diagnostics (pulls comtypes).
# Needs >=20251023: GetAllDevices(data_flow, device_state) params and
# AudioDevice.AudioSessionManager first shipped in that release.
pycaw>=20251023 ; sys_platform == 'win32'

# Development dependencies (optional)
# pytest>=7.0.0            # Testing
# black>=23.0.0            # Code formatting
# mypy>=1.0.0              # Type checking
